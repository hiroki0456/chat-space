.side-bar
  .side-bar__user-name
    .side-bar__user-name__names
      = current_user.name
    .side-bar__user-name__icons
      =link_to new_group_path do
        =icon('fa fa-edit','edit', class: 'side-bar__user-name__icons')
      = link_to edit_user_path(current_user) do
        = icon('fas', 'cog', class:'side-bar__user-name__icons')
  .side-bar__group-list
    - current_user.groups.each do |group|
      .side-bar__group-list__group
        = link_to group_messages_path(group) do
          .side-bar__group-list__group__name
            aaa
          .side-bar__group-list__group__message
            まだメッセージがありません